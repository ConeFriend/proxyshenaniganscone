{
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ],
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "npm install --legacy-peer-deps && npm run build"
      }
    }
  ]
}
